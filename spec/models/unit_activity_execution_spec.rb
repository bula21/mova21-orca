# frozen_string_literal: true

require 'rails_helper'

RSpec.describe UnitActivityExecution, type: :model do
  # let(:activity_execution) { create(:activity_execution, max_amount_participants: 50) }

  # describe '#amount_participants' do
  #   subject(:amount_participants) { activity_execution.amount_participants }
  #   before do
  #     create_list(:unit_activity_execution, 4, activity_execution: activity_execution, headcount: 10)
  #   end

  #   it { is_expected.to be 40 }
  # end
end
